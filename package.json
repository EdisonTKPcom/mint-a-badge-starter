{
  "name": "mint-a-badge",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build:fe": "cd frontend && npm run build",
    "preview": "cd frontend && npm run preview",
    "test": "hardhat test",
    "compile": "hardhat compile",
    "deploy:sepolia": "hardhat run script/deploy.ts --network sepolia",
    "deploy:local": "hardhat run script/deploy.ts"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@types/node": "^20.12.12",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2"
  }
}